ResetUserPassword:
  handler: bin/reset_user_password
  events:
    - httpApi:
        path: /reset/user/password
        method: post
        authorizer: cognitoAuthorizer
  timeout: 10
  environment:
    CLIENT_POOL_ID: !Ref CognitoUserPoolClient
    USER_POOL_ID: !Ref CognitoUserPool
