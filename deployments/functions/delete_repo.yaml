DeleteRepo:
  handler: bin/delete_repo
  package:
    artifact: archive/DeleteRepo.zip
  events:
    - httpApi:
        path: /delete/repo
        method: post
        authorizer: cognitoAuthorizer
  maximumRetryAttempts: 0
  environment:
    TABLE_NAME: ${self:service}-${self:provider.stage}
