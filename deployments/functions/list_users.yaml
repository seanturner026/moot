ListUsers:
  handler: bin/list_users
  package:
    artifact: archive/ListUsers.zip
  events:
    - httpApi:
        method: GET
        path: /list/users
        authorizer: cognitoAuthorizer
  maximumRetryAttempts: 0
  environment:
    USER_POOL_ID: !Ref CognitoUserPool
