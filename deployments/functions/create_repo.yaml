CreateRepo:
  handler: bin/create_repo
  package:
    artifact: archive/CreateRepo.zip
  events:
    - httpApi:
        path: /create/repo
        method: post
        authorizer: cognitoAuthorizer
  maximumRetryAttempts: 0
  environment:
    TABLE_NAME: ${self:service}-${self:provider.stage}
