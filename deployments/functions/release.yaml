Release:
  handler: bin/release
  package:
    artifact: archive/Release.zip
  events:
    - httpApi:
        path: /release
        method: post
        authorizer: cognitoAuthorizer
  timeout: 10
  environment:
    GITHUB_TOKEN: ${self:custom.GITHUB_TOKEN}
    WEBHOOK_URL: ${self:custom.WEBHOOK_URL_RELEASE_CHANNEL}
