DeleteUser:
  handler: bin/delete_user
  package:
    artifact: archive/DeleteUser.zip
  events:
    - httpApi:
        path: /delete/user
        method: post
        authorizer: cognitoAuthorizer
  maximumRetryAttempts: 0
  environment:
    USER_POOL_ID: !Ref CognitoUserPool
